# --- Django ---
SECRET_KEY=changeme
DEBUG=True

# --- Database (leave blank to use SQLite automatically) ---
# If you want PostgreSQL locally, fill these five:
DB_NAME=charityconnect
DB_USER=charityuser
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# --- Stripe (safe test keys or leave blank) ---
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_API_VERSION=2024-06-20

# --- Email (dev) ---
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# --- Celery/Redis (later iterations) ---
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/1

# --- AI toggle (off for Iteration 1) ---
AI_ENABLED=False
OPENAI_API_KEY=
